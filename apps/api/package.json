{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "migrate": "dbmate --wait --wait-timeout 30s up",
    "db:up": "docker compose up -d && npm run migrate",
    "db:down": "docker compose down -v",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch --env-file=.env src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/type-provider-zod": "^1.0.0",
    "close-with-grace": "^2.5.0",
    "fastify": "^5.10.0",
    "neverthrow": "^8.2.0",
    "pg": "^8.22.0",
    "uuidv7": "^1.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/pg": "^8.20.0"
  }
}
